{{yield}}
{{log 'job-view' job}}
<div class="container">
	<div class="row">
	  <div class="col s12 m12">
        <img src="{{job.thumbnail}}" class="img-responsive" alt="...">
        <h3 class="text-left">{{job.title}}</h3>
        <p class="text-left">{{{job.content}}}</p>
	  </div>
	</div>
</div>